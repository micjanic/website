import React from 'react'
import { createBuild } from '../sceneHelpers'
import BuildCell from './BuildCell'

export const BuildStage = ({ stage }) => {
    return <div>{stage.map((build) => {})}</div>
}
